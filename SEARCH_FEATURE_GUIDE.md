# 분실물 검색 기능 구현 완료

## ✅ 구현된 기능

### 1. 지역별 검색
- 서울시 전체 구(25개) 지원
- 드롭다운 메뉴로 간편하게 선택
- 습득장소와 보관장소 모두 검색

### 2. 카테고리별 검색
- 11개 주요 카테고리 지원
  - 지갑, 가방, 핸드폰, 노트북, 서류, 귀중품, 의류, 우산, 도서, 기타
- 버튼 클릭으로 선택
- 선택된 카테고리는 파란색으로 강조

### 3. 키워드 검색
- 전체 텍스트 검색 지원
- 제목, 설명, 카테고리, 색상, 위치에서 검색

### 4. 복합 검색
- 키워드, 카테고리, 지역을 자유롭게 조합
- 고급 검색 API 활용

### 5. 페이지네이션
- 페이지당 12개 항목
- 이전/다음 버튼
- 페이지 번호 직접 선택

### 6. 향상된 UI/UX
- 반응형 디자인 (모바일, 태블릿, 데스크톱)
- 로딩 스피너
- 에러 메시지 표시
- 검색 결과 개수 표시
- 상태별 배지 (습득, 수령완료, 만료 등)

## 📁 수정된 파일

### Frontend
1. **`frontend/src/lib/api.js`**
   - 검색 관련 API 함수 추가
   - `searchByRegion()`, `searchByCategory()`, `advancedSearch()` 등

2. **`frontend/src/pages/Search.jsx`**
   - 완전히 새로 작성
   - 지역/카테고리 선택 UI
   - 검색 로직 구현
   - 페이지네이션

3. **`frontend/src/components/ui/ItemCard.jsx`**
   - 백엔드 데이터 구조에 맞게 수정
   - 상태 배지, 카테고리, 위치 정보 표시
   - 날짜 포맷팅

4. **`frontend/src/pages/Home.jsx`**
   - 최근 분실물 표시
   - 검색 바에서 Search 페이지로 이동
   - 안내 섹션 추가

5. **`frontend/SEARCH_FEATURE_README.md`**
   - 상세 사용 가이드 문서

### Backend
백엔드는 이미 구현되어 있었습니다:
- `LostItemController.java` - 검색 API 엔드포인트
- `LostItemService.java` - 검색 비즈니스 로직
- `LostItemRepository.java` - 데이터베이스 쿼리

## 🚀 실행 방법

### 1. 환경 변수 설정
프론트엔드 디렉토리에 `.env` 파일을 생성하세요:

```bash
cd frontend
```

`.env` 파일 내용:
```env
VITE_API_BASE_URL=http://localhost:8080
```

### 2. 프론트엔드 실행
```bash
cd frontend
npm install
npm run dev
```

브라우저에서 http://localhost:5173 접속

### 3. 백엔드 실행
```bash
cd backend/capstone-backend
./mvnw spring-boot:run
```

Windows에서는:
```bash
mvnw.cmd spring-boot:run
```

## 📱 사용 방법

### 홈 페이지
1. 상단 검색 바에 키워드 입력
2. Enter 또는 검색 버튼 클릭
3. 자동으로 검색 페이지로 이동
4. 하단에 최근 등록된 분실물 6개 표시

### 검색 페이지 (`/search`)
1. **키워드 입력**: 원하는 키워드 입력 (선택사항)
2. **카테고리 선택**: 버튼 클릭으로 선택 (선택사항)
3. **지역 선택**: 드롭다운에서 선택 (선택사항)
4. **검색 버튼 클릭**: 조건에 맞는 결과 표시
5. **페이지 이동**: 하단 페이지네이션으로 다음/이전 페이지 이동

## 🎨 화면 구성

### 검색 폼
```
┌─────────────────────────────────────┐
│ 분실물 검색                          │
│ 지역과 카테고리를 선택하여 검색하세요   │
├─────────────────────────────────────┤
│ 키워드 검색                          │
│ [                    ] [검색]        │
│                                     │
│ 카테고리                             │
│ [전체] [지갑] [가방] [핸드폰] ...    │
│                                     │
│ 지역                                │
│ [▼ 전체                    ]        │
└─────────────────────────────────────┘
```

### 검색 결과
```
총 24개의 분실물 (1 / 2 페이지)

┌──────┐ ┌──────┐ ┌──────┐
│[습득] │ │[습득] │ │[습득] │
│지갑  │ │가방  │ │핸드폰│
│      │ │      │ │      │
│검은색 │ │빨간색 │ │흰색  │
│...   │ │...   │ │...   │
└──────┘ └──────┘ └──────┘

[이전] [1] [2] [다음]
```

## 🎯 API 엔드포인트

| 엔드포인트 | 설명 |
|-----------|------|
| `GET /api/lost-items` | 전체 목록 |
| `GET /api/lost-items/search?keyword={keyword}` | 키워드 검색 |
| `GET /api/lost-items/search/region?region={region}` | 지역 검색 |
| `GET /api/lost-items/category/{category}` | 카테고리 검색 |
| `GET /api/lost-items/search/advanced` | 고급 검색 |
| `GET /api/lost-items/search/fulltext?text={text}` | 전체 텍스트 검색 |
| `GET /api/lost-items/recent` | 최근 분실물 10개 |

## 🔍 검색 로직

### 1. 지역만 선택
→ `searchByRegion()` API 호출

### 2. 카테고리만 선택
→ `advancedSearch()` API 호출 (category 파라미터)

### 3. 키워드만 입력
→ `advancedSearch()` API 호출 (keyword 파라미터)

### 4. 지역 + 다른 조건
→ `searchFullText()` API 호출 (전체 텍스트 검색)

### 5. 조건 없음
→ `list()` API 호출 (전체 목록)

## 🎨 상태 배지 색상

| 상태 | 색상 | 의미 |
|------|------|------|
| FOUND | 초록색 | 습득 완료 |
| CLAIMED | 파란색 | 수령 완료 |
| EXPIRED | 빨간색 | 보관 기간 만료 |
| STORED | 노란색 | 보관 중 |
| RETURNED | 보라색 | 반환 완료 |
| DISPOSED | 회색 | 폐기 |

## 💾 데이터 출처 표시

- **사용자 등록**: 일반 사용자가 직접 등록한 분실물
- **경찰청 분실물**: LOST112에서 수집한 데이터
- **서울시 분실물**: 서울시 분실물센터 데이터

## ⚠️ 주의사항

1. **환경 변수 필수**: `.env` 파일을 반드시 생성해야 합니다
2. **백엔드 실행**: 프론트엔드 사용 전 백엔드 서버가 실행 중이어야 합니다
3. **포트 충돌**: 백엔드는 8080, 프론트엔드는 5173 포트를 사용합니다
4. **CORS**: 백엔드에서 CORS 설정이 되어 있어야 합니다

## 🐛 문제 해결

### API 호출 실패
```
원인: 백엔드 서버가 실행되지 않음
해결: backend/capstone-backend 디렉토리에서 mvnw spring-boot:run 실행
```

### 검색 결과 없음
```
원인: 데이터베이스에 데이터가 없음
해결: 백엔드의 데이터 수집 스크립트 실행 또는 테스트 데이터 삽입
```

### 페이지 로딩 오류
```
원인: .env 파일이 없거나 잘못된 API URL
해결: frontend/.env 파일 생성 및 올바른 API URL 설정
```

## 📝 추가 개발 아이디어

1. **날짜 범위 검색**: 습득일 기간 선택
2. **색상 필터**: 분실물 색상으로 필터링
3. **정렬 옵션**: 최신순, 오래된순, 인기순
4. **북마크 기능**: 관심 분실물 저장
5. **알림 기능**: 새로운 분실물 알림
6. **상세 페이지**: 분실물 상세 정보 모달
7. **지도 표시**: 습득 장소를 지도에 표시
8. **이미지 업로드**: 분실물 사진 첨부

## 📞 지원

문제가 발생하거나 질문이 있으시면:
1. `frontend/SEARCH_FEATURE_README.md` 상세 가이드 참조
2. 백엔드 로그 확인
3. 브라우저 개발자 도구 콘솔 확인

